{% extends "GloryUserBundle:Admin:layout.html.twig" %}

{% block content %}
    <div class="user_list">
        <ul>
            {% for user in users %}

                <li><a href="{{ path('glory_user_manage_show', {'id': user.id}) }}">{{user.username}}</a></li>

            {% endfor %}
        </ul>
    </div>
{% endblock %}
