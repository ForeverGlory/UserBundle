{% extends "GloryUserBundle::layout.html.twig" %}

{% block content %}
    <ul>
        <li>
            <span>用户名</span> <a href="{{path("glory_user_profile_username")}}">{{user.username}}</a>
        </li>
        <li>
            <span>头像</span> <a href="{{path("glory_user_profile_avatar")}}"><img src="{{user.avatar}}"/></a>
        </li>
        <li>
            <span>密码</span> <a href="{{path("glory_user_profile_password")}}"></a>
        </li>
    </ul>
{% endblock content %}