{% extends "GloryUserBundle::layout.html.twig" %}

{% block content %}
    <form action="{{ path('glory_user_password') }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'change_password.submit'|trans }}" />
        </div>
    </form>
{% endblock content %}
